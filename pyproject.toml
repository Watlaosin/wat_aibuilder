[project]
name = "wat-aibuilder"
version = "0.1.0"
description = "Graph-based symbolic music analysis experiments"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "pytest>=9.0.2",

    "torch",
    "torch-geometric",
    "pandas",
    "partitura",
    "matplotlib",
    "networkx",
]